/**
 * main -starting program
(*
 *Description: prints the phrase and a new line)?
 *Return: return (0) required function
 */

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)

{
	char x;
	int j;

	for (j = 0; j < 10; j++)
		putchar (j % 10 + '0');
	for (x = 'a'; a <= 'f'; x++)
		putchar (x);
	putchar('\n');
	return (0);
}

